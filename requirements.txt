# Runtime dependencies of the plugins themselves.
# NOTE: the geopython/pygeoapi Docker image already ships psycopg2 and
# shapely as system packages, so the Dockerfile installs geoclip with
# --no-deps and does not need this file.
psycopg2-binary>=2.9
shapely>=2.0
# only needed for the gpkg and fgb output formats; the image has
# python3-fiona already
fiona>=1.9
